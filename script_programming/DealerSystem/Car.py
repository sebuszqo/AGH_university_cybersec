

class Car(object):
    def __init__(self, carId, clientId, brand, priceToSell,priceToRent,startDate, endDate ):
        self._carId = carId
        self._clientId = clientId
        self._brand = brand
        self._priceToSell = priceToRent
        self._priceToSell = priceToSell
        self._startDate= startDate
        self._endDate= endDate
